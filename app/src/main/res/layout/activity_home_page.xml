<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomePage">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="20dp">

    <ImageView
        android:id="@+id/gear"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:src="@drawable/gear"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/titleText1"
        android:layout_width="290dp"
        android:layout_height="wrap_content"
        android:text="Streamline your auto parts Experience"
        android:layout_centerHorizontal="true"
        android:textSize="24sp"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/gear"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/titleText2"
            android:layout_width="290dp"
            android:layout_height="wrap_content"
            android:text="Discover and manage automotive parts with ease, stay ahead of trends, optimize your inventory, and connect with trusted suppliers all in one place."
            android:layout_centerHorizontal="true"
            android:textSize="14sp"
            android:textAlignment="center"
            android:textColor="@android:color/black"
            android:layout_marginTop="30dp"
            android:layout_below="@+id/titleText1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <Button
            android:id="@+id/btn1"
            android:layout_width="180dp"
            android:layout_height="60dp"
            android:layout_below="@id/titleText2"
            android:layout_marginStart="18dp"
            android:layout_marginTop="50dp"
            android:background="@drawable/custom_button"
            android:text="Login"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="16sp"
            app:backgroundTint="@null" />

        <Button
            android:id="@+id/btn2"
            android:layout_width="180dp"
            android:layout_marginStart="215dp"
            android:layout_height="60dp"
            android:layout_below="@id/titleText2"
            android:layout_marginTop="50dp"
            android:background="@drawable/custom_buttom_register"
            android:backgroundTint="@null"
            android:text="Sign Up"
            android:textAllCaps="false"
            android:textColor="@color/black"
            app:backgroundTint="@null"
            android:textSize="16sp" />

    </RelativeLayout>

</ScrollView>